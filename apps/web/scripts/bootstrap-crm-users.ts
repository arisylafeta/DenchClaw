import { hashPassword, ALLOWED_EMAILS } from "../lib/auth"; import { queryPg } from "../lib/postgres";
async function main(){const password=process.env.CRM_BOOTSTRAP_PASSWORD;if(!password){throw new Error("CRM_BOOTSTRAP_PASSWORD must be injected at runtime and is never persisted by this script");} if(password.length<12)throw new Error("CRM_BOOTSTRAP_PASSWORD is too short"); const hash=await hashPassword(password); for(const email of ALLOWED_EMAILS) await queryPg(`insert into crm_users(email,display_name,password_hash) values($1,$3,$2) on conflict(email) do update set password_hash=excluded.password_hash,is_active=true,failed_login_count=0,locked_until=null,updated_at=now()`,[email,email.startsWith("ari")?"Ari":"Alex",hash]); console.log("Bootstrapped allowlisted CRM users");} main().catch(e=>{console.error(e instanceof Error?e.message:"bootstrap failed");process.exit(1)});
